"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useOpenWithRecord = void 0;

var _react = require("react");

var _open_with_record = require("../open_with_record");

/**
 * A hook for registering a callback to handle "open with record" events (from button field).
 *
 * Should only be used once in your block, ideally in the top level component: will throw otherwise.
 * Your block will not receive "open with record" events until a callback is registered.
 *
 * TODO(emma): write better documentation when we make this public
 *
 * @hidden
 */
var useOpenWithRecord = callback => {
  // registerOpenWithRecordCallback returns the unsubscribe function.
  (0, _react.useEffect)(() => (0, _open_with_record.registerOpenWithRecordCallback)(callback), [callback]);
}; // TODO(emma): switch to default export when exposing outside of private utils


exports.useOpenWithRecord = useOpenWithRecord;